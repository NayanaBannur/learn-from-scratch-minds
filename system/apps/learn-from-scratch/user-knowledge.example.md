# User knowledge

> **Template.** Copy this file to `user-knowledge.md` (gitignored) and edit it to reflect
> what *you* already know. The `learn-from-scratch` skill reads `user-knowledge.md`, not
> this file.
>
> ```bash
> cp user-knowledge.example.md user-knowledge.md
> ```

The reader's existing foundational knowledge. The `learn-from-scratch` skill **reads** this
as the assumed floor: terms and concepts listed here are used directly in generated content
— never re-taught, defined, or glossed. It is the baseline the reader already commands.

Entries are **terms and short phrases**, grouped by field — not sentences or explanations.
Add, remove, or replace sections to match your own background.

**Updating this file:** the skill may only **add** to it with the user's **explicit
approval** — the user must confirm they know a topic before it is recorded here. The skill
must never add entries on its own assumption.

## Machine learning / AI

- neural network, layer, activation function
- weights, parameters, bias
- gradient, gradient descent, backpropagation
- loss / objective function
- optimizer, SGD, Adam, AdamW, learning rate
- training step, epoch, batch, mini-batch
- training vs inference
- overfitting, underfitting, regularization
- supervised / unsupervised / self-supervised learning
- classification, regression
- transformer, attention, self-attention
- token, tokenization, embedding
- logits, softmax, temperature
- GPU, tensor, matrix multiplication
- dataset, train/validation/test split
- hyperparameter, fine-tuning, pretraining
